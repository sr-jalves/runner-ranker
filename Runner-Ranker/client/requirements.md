## Packages
framer-motion | Complex animations for the timer and page transitions
recharts | Visualizing running statistics and progress
date-fns | Formatting dates and durations
react-circular-progressbar | For the interval timer visualization

## Notes
- Theme: High contrast dark mode (black background) with neon accents (#ccff00, #00ffff, #ff00ff).
- Fonts: Inter (UI) and JetBrains Mono (Numbers/Data).
- Auth: Using Replit Auth (useAuth hook).
- API: /api/activities (POST/GET), /api/leaderboard (GET).
